import 'package:flutter/material.dart';

void main() {
  runApp(const PurpleLogin());
}
class PurpleLogin extends StatelessWidget {
  const PurpleLogin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
